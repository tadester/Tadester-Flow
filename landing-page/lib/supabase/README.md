# Supabase Helpers

This folder contains the landing page's Supabase client setup.

## Current Files

- `browser-client.ts`
- `server-client.ts`

## Why It Exists

It centralizes environment-variable access and prevents the rest of the landing-page code from re-creating Supabase clients in multiple places.
