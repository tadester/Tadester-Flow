# Landing Page Helpers

This folder contains non-visual support code for the landing page.

## Structure

- `supabase/`: Supabase clients and helpers
- `validations/`: form validation logic

## Rule

If code is not a page and not a component, it likely belongs here or in `types/`.
