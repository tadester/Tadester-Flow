# Landing Page API

This folder contains App Router API endpoints for the marketing site.

## Current Endpoint

- `waitlist/`: public submission route for lead capture

## Why It Exists

The landing page needs a very small server surface of its own for waitlist collection without depending on the larger backend service.
