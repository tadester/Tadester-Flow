# UI Components

This folder contains smaller reusable controls used across the landing page.

## Current Components

- `Button.tsx`
- `Input.tsx`
- `Select.tsx`
- `StatusMessage.tsx`

These are the pieces that help keep the waitlist form and CTA behavior consistent.
