# Landing Page Components

This folder contains reusable UI building blocks for the marketing site.

## Structure

- `layout/`: shared frame elements such as navbar and footer
- `sections/`: homepage-level content blocks
- `ui/`: low-level reusable controls

## Why This Split Matters

It keeps the homepage readable and prevents one giant page file from owning every piece of the interface.
