# Landing Page Supabase Migrations

This folder is reserved for landing-page-specific migration history if the marketing site ever needs isolated Supabase changes.

In practice, the main operational schema should stay under `backend/supabase/migrations/`.
