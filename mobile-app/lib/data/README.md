# Mobile Data Layer

This folder is intended to hold data-access concerns for the Flutter app.

## Structure

- `local/`: on-device persistence
- `remote/`: HTTP or Supabase access
- `repositories/`: app-facing abstractions

## Current Maturity

This layer is exploratory on this branch. Some files are placeholders rather than finished runtime code.
