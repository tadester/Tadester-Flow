# Drift Storage

This folder is reserved for Drift database setup and table definitions.

## Intended Use

- define local tables
- configure the on-device database
- support offline persistence and sync staging

On this branch, it should be treated as an early foundation area rather than a finished persistence layer.
