# Mobile Supabase Functions

This folder contains Supabase Edge Functions related to the mobile workspace.

## Why It Exists

It provides a place for mobile-adjacent serverless helpers without mixing them into Flutter UI code.
