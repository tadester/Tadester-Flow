# Mobile Supabase Migrations

This folder is reserved for migrations owned by the mobile workspace.

Use caution here. If a change affects the core operational data model, it should usually live under `backend/supabase/migrations/` instead.
